APP_VERSION=1.0.0
MODEL_PATH="src/model/artifacts/model.pkl"
RECOMMENDATION_TOP_K=4
EVENTS_PATH="data/events.csv"
PRODUCTS_PATH="data/products.csv"
APP_HOST="0.0.0.0"
APP_PORT=8080

# Optional OpenTelemetry instrumentation (see entrypoint.sh / compose.yaml).
# Leave ENABLE_OTEL unset or "false" to run without any tracing overhead.
# ENABLE_OTEL=true
# OTEL_SERVICE_NAME=recommendation_api
# OTEL_EXPORTER_OTLP_ENDPOINT=http://otel:4317